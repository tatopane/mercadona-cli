[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mercadona-cli"
version = "1.0.0"
dependencies = ["click>=8.0", "requests>=2.28"]

[project.scripts]
mercadona = "mercadona:cli"
